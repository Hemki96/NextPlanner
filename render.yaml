services:
  - type: web
    name: nextplanner
    env: node
    plan: free
    buildCommand: ""
    startCommand: "npm start"
    autoDeploy: true
    healthCheckPath: /readyz
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXTPLANNER_DATA_DIR
        value: /data
      - key: NEXTPLANNER_LOGIN_USER
        value: admin
      - key: NEXTPLANNER_LOGIN_PASSWORD
        generateValue: true
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
