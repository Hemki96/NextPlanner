# CI API Test Evidence

Status: PENDING

## Required for Gate B1/B3/B4

- Workflow: `NextPlanner2 API Tests`
- File: `../.github/workflows/nextplanner2-api-tests.yml`
- Required result: `api-tests` job SUCCESS

## Evidence fields

- Run date:
- Run URL:
- Commit SHA:
- Branch:
- Job result:
- Step `Run API integration tests` result:
- Artifact `nextplanner2-api-test-output` checked:
- Test summary snippet:

## Acceptance

Set status to `PASS` only if the workflow run is green and the integration test step succeeded.
